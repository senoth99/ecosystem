services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: drops
      POSTGRES_PASSWORD: drops
      POSTGRES_DB: drops
    ports:
      - "5432:5432"
    volumes:
      - drops_pg:/var/lib/postgresql/data

volumes:
  drops_pg:

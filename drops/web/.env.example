# PostgreSQL (локально: docker compose up -d в корне репозитория)
DATABASE_URL="postgresql://drops:drops@127.0.0.1:5432/drops?schema=public"
APP_PASSWORD="1234"
# Случайная длинная строка в продакшене (подпись сессии)
SESSION_SECRET="change-me"
# PORT=3000
